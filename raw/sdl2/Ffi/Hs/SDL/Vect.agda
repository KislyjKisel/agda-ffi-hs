{-# OPTIONS --without-K #-}

module Ffi.Hs.SDL.Vect where

{-# WARNING_ON_IMPORT "unimplemented: requires preprocessor" #-}

postulate
    V2 : ∀{aℓ} → Set aℓ → Set aℓ
    V4 : ∀{aℓ} → Set aℓ → Set aℓ
